[General]
# Every key here names a file that ships in this directory (tests/sddm-theme-test.sh checks that).
# backdrop.png: the dark weave wallpaper, pre-blurred and dimmed (1920x1080, cropped to the panel by the QML).
# background.png (unblurred 4K wallpaper) is also installed at build time for the KSplash lock-screen pairing; the greeter
# uses the blurred one on purpose.
background=backdrop.png
