# Fab OS boot hand-off: the splash stays up (plymouth-quit* are masked) until the login manager is about to start,
# then Plymouth quits keeping its last frame on screen, so the greeter fades in with no console in between.
[Service]
ExecStartPre=-/usr/bin/plymouth quit --retain-splash
