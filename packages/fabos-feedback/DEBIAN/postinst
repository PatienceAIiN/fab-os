#!/bin/sh
set -e
case "$1" in configure)
  chmod 0600 /etc/fabos/feedback.env || true
  systemctl daemon-reload 2>/dev/null || true
  systemctl enable fabos-feedback.socket 2>/dev/null || true
  systemctl start fabos-feedback.socket 2>/dev/null || true
;; esac
exit 0
