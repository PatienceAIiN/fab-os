# @DISTRO_NAME@ feature/branding/AI updates from @VENDOR_NAME@ (signed; apt verifies every Release file).
# Ubuntu updates continue to come from the Ubuntu archive unchanged.
Types: deb
URIs: https://fabos.patienceai.in/apt
Suites: @DISTRO_CODENAME@
Components: main
Signed-By: /usr/share/keyrings/fabos-archive-keyring.gpg
