---
# Fab OS installer: umount module (last exec job). "emergency: true" makes it run even after an earlier job failed, so a
# failed installation never leaves the target mounted under the live session.
emergency: true
