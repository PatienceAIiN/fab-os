# /etc/hostname, /etc/hosts and /etc/resolv.conf are bind-mounted during podman build, so RUN writes are lost;
# the overlay COPY provides hostname/hosts and this rule creates the resolved stub symlink at boot.
L+ /etc/resolv.conf - - - - ../run/systemd/resolve/stub-resolv.conf
